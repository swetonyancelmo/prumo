import { Module } from '@nestjs/common';

/**
 * Placeholder — implementado na Fase 3 (webhook Evolution API + fila BullMQ).
 * Mantido aqui para respeitar a estrutura de módulos definida no CLAUDE.md.
 */
@Module({})
export class WhatsappModule {}
