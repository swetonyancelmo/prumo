import { Module } from '@nestjs/common';

/**
 * Placeholder — implementado na Fase 4 (Claude API com tool use para extração
 * estruturada de despesas/receitas/tarefas). Estrutura reservada conforme CLAUDE.md.
 */
@Module({})
export class AiModule {}
